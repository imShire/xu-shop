# views/shipping

> 阶段 4。对应 arch 08。

## 文件
- `PendingList.vue`：待发货列表，多选 → 批量发货
- `ShippedList.vue`：已发货列表，含改单
- `SenderAddress.vue`：发件地址 CRUD（默认唯一）
- `CarrierConfig.vue`：快递公司启用 / 月结账号
- `BatchShipDialog.vue`：批量发货向导，含进度条 + 失败列表 + 合并 PDF 下载
